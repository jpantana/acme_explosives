// import typesData from '../../helpers/data/typesData';

// // const writeTypes = (types) => {
// //   console.error(types);
// // };

// const initTypes = (categoryId) => {
//   typesData.loadTypesForCard(categoryId)
//     .then((types) => {
//       writeTypes(types);
//     })
//     .catch(err => console.error(err));
// };

// export default { initTypes };
